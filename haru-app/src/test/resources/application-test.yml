spring:
  datasource:
    url: jdbc:h2:mem:testdb
    driver-class-name: org.h2.Driver
    username: sa
    password:
  mail:
    host: dummy
    port: 1025
    username: dumb
  security:
    oauth2:
      client:
        registration:
          google:
            client-id: "dummy-client-id"
            client-secret: "dummy-client-secret"

hashid:
  salt: dummy-salt
  min-length: 16

jwt:
  secret: "test-jwt-token"
  access-expiration: 60      # 60분
  refresh-expiration: 20160   # 20160분 = (60m * 24h) * 14days : 24시간 * 14일 = 2주
  header: "Authorization"

api:
  stt-api-url: http://localhost:8000/stt

  score-api-url: http://localhost:8001/api/v1/score_utterance

  openai:
    api-key: test_api_key

cloud:
  aws:
    s3:
      bucket: haru-it-bucket
    region:
      static: ap-northeast-2
    credentials:
      accessKey: dummy-key
      secretKey: dummy-secret

invite-url: invite-url

survey-url: survey-url

queue-name: queue-name

google-login-frontend-url: google-login-frontend-url

instagram:
  client:
    id: "dummy-client-id"
    secret: "dummy-client-secret"
  redirect:
    uri-onboarding: "dummy-redirect-uri"
    uri-workspace: "dummy-redirect-uri"